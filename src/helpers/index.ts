// Exportar todas las funciones del helper de storage
export {
  setStorage,
  getStorage,
  removeStorage,
  clearStorage,
  getStorageKeys,
  getStorageSize,
  hasStorage,
} from "./storage";

export type { StorageOptions, StorageData, UseStorageReturn } from "./storage";

